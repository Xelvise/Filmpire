import { makeStyles } from "@mui/styles";

export default makeStyles(theme => ({
    castImage: {
        width: "100%",
        maxWidth: "7em",
        height: "8em",
        objectFit: "cover",
        borderRadius: "10px",
    },
}));
